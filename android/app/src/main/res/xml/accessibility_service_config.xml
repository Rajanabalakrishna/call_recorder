<?xml version="1.0" encoding="utf-8"?>
<!-- File: android/app/src/main/res/xml/accessibility_service_config.xml -->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="0" />

    <!--
    SIMPLIFIED CONFIG FOR MIUI/POCO COMPATIBILITY:
    - typeAllMask: Accept all event types (required for service to be recognized)
    - canRetrieveWindowContent="false": We don't need to read screen content
    - notificationTimeout="0": Immediate event processing
    - No packageNames filter: MIUI doesn't like package filtering
    - Minimal flags: Only what's required

    This makes MIUI recognize the service as valid and allow it to run.
    -->